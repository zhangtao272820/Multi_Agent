# 智慧养老实训本地库（MySQL `p2026`，与 DB_Agent 当前库一致）

默认只读：`person_*`、`remote_*`、`elderly_*`、`bed_*`、`floor_*`、`org_*`、`monitor_*`、`teaching_*`、`cultivate_*`、`device_*`、`iot_*`；系统表仅 `sys_user` / `sys_role` / `sys_dept` / `sys_department` / `sys_menu`。
禁止查密码列、代码生成表、工作流表、未授权日志表。
人员主表是 `person_info`（有效行通常 `deleted = 0`）。健康指标走 `person_health_records`（按 `person_id` JOIN）。
足底压力主表 `remote_activity_foot_log`；仅当问区域/分区/重心时才 JOIN `remote_activity_foot_measure_log`。
情绪识别仪 → `remote_psychology_mood`；慢性病护理实训 → `remote_nursing_chronic`；骨密度 → `remote_health_bone_log`。
问数量且问「分别是什么」时，选出名称列，不要只 COUNT。性别枚举：`is_gender` 1=男 2=女。

## 表目录（白名单）

- `bed_info` 。列：id, bed_number, floor_id, elderly_id, status
- `cultivate_catalog` 资源目录。列：Id, Code, Name, ParentId, IsEnable
- `cultivate_resource` 课程资源。列：ResourceId, Type（资源类型  新增类型：封面999）, UUID（外部对接Id）, Name（资源名称）, ResourceCatalog（资源所属目录）, ZIPTreeConstructionPath（zip包中的文件路径）, Content（资源内容）, ImgPath（资源预览图）, SourceFileName（资源源路径）, TargetPhysicalPath（资源物理路径）, TargetRelativePath（资源相对路径）, IsShoped（是否已购买）
- `cultivate_resourceupload` 资源上传记录。列：Id, FileName（文件名称）
- `device_alarm_info` 报警信息。列：id（主键ID）, device_id（设备ID）, device_code（设备编码）, device_model_id（设备型号ID）, device_model_name（设备型号）, is_alarm_level（报警等级：4低 / 3中 / 2高 / 1紧急）, is_alarm_type（报警类型）, alarm_content（报警内容或描述信息）, alarm_time（报警触发时间）, is_handle_status（报警状态：1未处理、2处理中、3已处理、0取消）, is_source_type（来源类型：设备1 / 数字人2）, ai_id（数字人ID（可选））, device_person_id（报警相关人员ID（如老人ID））, device_person_name（报警相关人员）
- `device_alarm_rule` 报警规则。列：id（主键ID）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, is_alarm_type（报警类型）, is_yn_enable（是否启用）, alarm_name（报警名称）, condition_json（条件json）
- `device_bp_log` 设备-智能血压仪-数据接收记录。列：id（主键;）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, device_model_id（设备型号ID）, device_model_name（设备型号名称）, heart_rate（心率 (次/分钟)）, systolic_pressure（收缩压 (高压)）, diastolic_pressure（舒张压 (低压)）, is_yn_arrhythmia（是否心率不齐）
- `device_bp_maibobo_log` 设备-智能血压仪（脉搏波）-数据接收记录。列：id（主键;）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, device_model_id（设备型号ID）, device_model_name（设备型号名称）, heart_rate（心率 (次/分钟)）, systolic_pressure（收缩压 (高压)）, diastolic_pressure（舒张压 (低压)）
- `device_classify` 设备分类。列：id, create_by（创建人ID）, create_time, update_by（修改人ID）, update_time, deleted（逻辑删除标志）, tenant_id（租户ID）, code（编号）, name（名称）
- `device_command_temp` 设备类型。列：id（主键;）, create_by（创建人ID）, create_time, update_by（修改人ID）, update_time, deleted（逻辑删除标志）, tenant_id（租户ID）, name（命令名称）, device_model_id（设备型号id）, command_bean_name（执行方法命名）, param_temp_json（参数模板）, isallow_connect_central（是否允许连接中控）
- `device_ctd_child_button_log` 设备-驰通达-手动按钮报警器-数据接收记录。列：id（主键;）, is_remote_message_from（数据来源）, org_building_id（所属楼栋id）, org_floor_id（所属楼层id）, org_room_id（所属房间id）, org_bed_id（所属床位id）, device_id（设备ID）, is_device_state（设备状态）, device_state_name（设备状态名称）, is_online_status（在线状态;1在线 0离线）, online_status_name（设备在线状态）, device_model_id（设备型号ID）, device_model_name（设备型号名称）, longitude（经度）
- `device_ctd_exist_log` 设备-驰通达-人体存在雷达-数据接收记录。列：id（主键;）, is_remote_message_from（数据来源）, org_building_id（所属楼栋id）, org_floor_id（所属楼层id）, org_room_id（所属房间id）, org_bed_id（所属床位id）, device_id（设备ID）, is_device_state（设备状态）, device_state_name（设备状态名称）, is_online_status（在线状态;1在线 0离线）, online_status_name（设备在线状态）, device_model_id（设备型号ID）, device_model_name（设备型号名称）, longitude（经度）
- `device_ctd_host_log` 设备-驰通达-拉绳网关报警器-数据接收记录。列：id（主键;）, is_remote_message_from（数据来源）, org_building_id（所属楼栋id）, org_floor_id（所属楼层id）, org_room_id（所属房间id）, org_bed_id（所属床位id）, device_id（设备ID）, is_device_state（设备状态）, device_state_name（设备状态名称）, is_online_status（在线状态;1在线 0离线）, online_status_name（设备在线状态）, device_model_id（设备型号ID）, device_model_name（设备型号名称）, longitude（经度）
- `device_ctd_jm_log` 设备-驰通达-一氧化碳报警器-数据接收记录。列：id（主键;）, is_remote_message_from（数据来源）, org_building_id（所属楼栋id）, org_floor_id（所属楼层id）, org_room_id（所属房间id）, org_bed_id（所属床位id）, device_id（设备ID）, is_device_state（设备状态）, device_state_name（设备状态名称）, is_online_status（在线状态;1在线 0离线）, online_status_name（设备在线状态）, device_model_id（设备型号ID）, device_model_name（设备型号名称）, signal_value（信号值）
- `device_ctd_jt_log` 设备-驰通达-燃气报警器-数据接收记录。列：id（主键;）, is_remote_message_from（数据来源）, org_building_id（所属楼栋id）, org_floor_id（所属楼层id）, org_room_id（所属房间id）, org_bed_id（所属床位id）, device_id（设备ID）, is_device_state（设备状态）, device_state_name（设备状态名称）, is_online_status（在线状态;1在线 0离线）, online_status_name（设备在线状态）, device_model_id（设备型号ID）, device_model_name（设备型号名称）, signal_value（信号值）
- `device_ctd_rope_log` 设备-驰通达-拉绳报警器-数据接收记录。列：id（主键;）, is_remote_message_from（数据来源）, org_building_id（所属楼栋id）, org_floor_id（所属楼层id）, org_room_id（所属房间id）, org_bed_id（所属床位id）, device_id（设备ID）, is_device_state（设备状态）, device_state_name（设备状态名称）, is_online_status（在线状态）, online_status_name（设备在线状态）, device_model_id（设备型号ID）, device_model_name（设备型号名称）, longitude（经度）
- `device_ctd_sleep_log` 设备信息-驰通达睡眠雷达-睡眠记录。列：id（主键;）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, device_code（设备编号/ID）, device_model_name（设备类型名称）, heart_rate（心率 (次/分钟)）, breath_rate（呼吸频率）, breath_state_name（呼吸状态）
- `device_ctd_smoke_log` 设备-驰通达-烟雾报警器-数据接收记录。列：id（主键;）, is_remote_message_from（数据来源）, org_building_id（所属楼栋id）, org_floor_id（所属楼层id）, org_room_id（所属房间id）, org_bed_id（所属床位id）, device_id（设备ID）, is_device_state（设备状态）, device_state_name（设备状态名称）, is_online_status（在线状态;1在线 0离线）, online_status_name（设备在线状态）, device_model_id（设备型号ID）, device_model_name（设备型号名称）, longitude（经度）
- `device_ctd_water_log` 设备-驰通达-水浸报警器-数据接收记录。列：id（主键;）, is_remote_message_from（数据来源）, org_building_id（所属楼栋id）, org_floor_id（所属楼层id）, org_room_id（所属房间id）, org_bed_id（所属床位id）, device_id（设备ID）, is_device_state（设备状态）, device_state_name（设备状态名称）, is_online_status（在线状态;1在线 0离线）, online_status_name（设备在线状态）, device_model_id（设备型号ID）, device_model_name（设备型号名称）, longitude（经度）
- `device_gear_fit_log` 设备-智能手环-数据接收记录。列：id（主键;）, create_by（创建人ID）, create_time, update_by（修改人ID）, update_time, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, device_code（设备编号/ID）, heart_rate（心率 (次/分钟)）, blood_oxygen（血氧饱和度 (百分比)）, temperature（温度 (摄氏度)）, blood_pressure（血压 (高压,低压)）, systolic_pressure（收缩压 (高压)）
- `device_info` 设备信息。列：id（主键;）, device_model_id（设备型号ID）, device_classify_id（分类id）, device_photo_id（设备照片id）, name（设备名称;）, code（设备编号/ID）, mac（mac地址）, sn（设备SN码）, is_device_state（设备状态）, install_date（安装时间;）, is_online_status（在线状态;1在线 0离线）, firmware_ver（固件版本;）, ip_address（IP地址;）, location_desc（安装位置描述;）
- `device_info_crutch` 设备信息-智能拐杖。列：id（主键;）, create_by（创建人ID）, create_time, update_by（修改人ID）, update_time, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, device_code（设备编号/ID）, device_model_name（设备类型名称）, longitude（经度）, latitude（纬度）, address（地址）, iccm_card_id（icc卡ID）
- `device_info_ctd_badge` 设备信息-驰通达胸牌。列：id（主键;）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, device_code（设备编号/ID）, device_model_name（设备类型名称）, longitude（经度）, latitude（纬度）, address（地址）, iccm_card_id（icc卡ID）
- `device_info_ctd_host` 设备信息-驰通达报警主机。列：id（主键;）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, device_code（设备编号/ID）, device_model_name（设备类型名称）, iccm_card_id（icc卡ID）, icc_code（电话卡编码）, icc_supplier_id（ICC卡供应商id）, phone_number_first（电话1）
- `device_info_ctd_jm` 设备信息-驰通达一氧化碳报警器。列：id（主键;）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, device_code（设备编号/ID）, device_model_name（设备类型名称）, signal_value（信号值）, concentration（气体浓度）
- `device_info_ctd_jt` 设备信息-驰通达燃气报警器。列：id（主键;）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, device_code（设备编号/ID）, device_model_name（设备类型名称）, signal_value（信号值）, concentration（气体浓度）
- `device_info_ctd_sleep` 设备信息-驰通达睡眠雷达。列：id（主键;）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, device_code（设备编号/ID）, device_model_name（设备类型名称）, heart_rate（心率 (次/分钟)）, breath_rate（呼吸频率）, breath_state_name（呼吸状态）
- `device_info_gear_fit` 设备信息-智能手环。列：id（主键;）, create_by（创建人ID）, create_time, update_by（修改人ID）, update_time, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, device_code（设备编号/ID）, device_model_name（设备类型名称）, longitude（经度）, latitude（纬度）, address（地址）, heart_rate（心率 (次/分钟)）
- `device_info_mattress` 设备信息-智能床垫。列：id（主键;）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, device_code（设备编号/ID）, device_model_name（设备类型名称）, heart_rate（心率 (次/分钟)）, breath_rate（呼吸频率）, status_name（人体状态）
- `device_info_tigear` 设备信息-钛极智能床垫。列：id（主键;）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, device_code（设备编号/ID）, device_model_name（设备类型名称）, heart_rate（心率 (次/分钟)）, breath_rate（呼吸频率）, product_id（产品id（远程调用参数））
- `device_info_tuya_central` 涂鸦中控。列：id（主键;）, create_by（创建人ID）, create_time, update_by（修改人ID）, update_time, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, child_device_id（子设备ID）, is_button_type（按钮类型）, command_temp_id（命令ID）
- `device_info_tuya_doorlock` 涂鸦智能门锁。列：id（主键;）, create_by（创建人ID）, create_time, update_by（修改人ID）, update_time, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, lock_password（门锁密码）
- `device_info_tuya_meter` 涂鸦智能电表。列：id（主键;）, create_by（创建人ID）, create_time, update_by（修改人ID）, update_time, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, voltage（电压）, current（电流）, power（电功率）
- `device_mattress_log` 设备信息-智能床垫-睡眠记录。列：id（主键;）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, device_code（设备编号/ID）, device_model_name（设备类型名称）, heart_rate（心率 (次/分钟)）, breath_rate（呼吸频率）, status_name（人体状态）
- `device_model` 设备类型。列：id（主键;）, name（设备类型名称;）, code（设备类型编码;）, vendor_id（厂商ID;）, label（设备标签列表）, device_type（设备大类;）, protocol（通信协议;）, description（型号说明;）, create_by（创建人ID）, create_time, update_by（修改人ID）, update_time, deleted（逻辑删除标志）, tenant_id（租户ID）
- `device_model_menu` 设备类型菜单项。列：id（主键;）, create_by（创建人ID）, create_time, update_by（修改人ID）, update_time, deleted（逻辑删除标志）, tenant_id（租户ID）, name（页面名称）, sort（排序）, is_device_model_menu（菜单类型）, is_yn_visible（是否隐藏）, device_model_id（设备类型ID）
- `device_tigear_log` 设备信息-钛极智能床垫-睡眠记录。列：id（主键;）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, device_code（设备编号/ID）, device_model_name（设备类型名称）, breathe（呼吸频率）, heartrate（心率）
- `device_tigear_report_log` 钛极睡眠报告记录。列：id（主键ID）, start_time（睡眠开始时间）, end_time（睡眠结束时间）, total_time（睡眠时长）, onbed_time（在床时间）, leavebed_time（离床时长）, leavebed_number（离床次数）, deepsleep_time（深睡时长）, deepsleep_ratio（深睡比例）, lightsleep_time（浅睡时长）, move_number（体动次数）, snore_number（打鼾次数）, heartbeat_avg（平均心率）, bradycardia_time（心率过缓时长）
- `device_tuya_doorlock_log` 设备-涂鸦门锁-历史记录。列：id（主键;）, is_remote_message_from（数据来源）, org_building_id（所属楼栋id）, org_floor_id（所属楼层id）, org_room_id（所属房间id）, org_bed_id（所属床位id）, device_id（设备ID）, is_device_state（设备状态）, device_state_name（设备状态名称）, device_model_id（设备型号ID）, device_model_name（设备型号名称）, create_by（创建人）, create_time（创建时间）, update_by（修改人）
- `device_tuya_exist_log` 设备-涂鸦人存-数据接收记录。列：id（主键;）, create_by（创建人ID）, create_time, update_by（修改人ID）, update_time, deleted（逻辑删除标志）, tenant_id（租户ID）, device_id（设备ID）, is_device_state（设备状态）, device_state_name（设备状态名称）, device_model_id（设备型号ID）, device_model_name（设备型号名称）, org_building_id（所属楼栋id）, org_floor_id（所属楼层id）
- `device_tuya_meter_log` 设备-涂鸦电表-数据接收记录。列：id（主键;）, create_by（创建人ID）, create_time, update_by（修改人ID）, update_time, deleted（逻辑删除标志）, tenant_id（租户ID）, voltage（电压）, current（电流）, power（电功率）, device_id（设备ID）, is_device_state（设备状态）, device_state_name（设备状态名称）, device_model_id（设备型号ID）
- `elderly_info` 。列：id, name, age, gender, health_status, contact_info, admission_date
- `floor_info` 。列：id, name, description
- `iot_receive_bp_log` 智能血压计数据表。列：id（主键ID）, imei（设备IMEI号）, tel（设备里的手机卡号码）, iccid（设备ICCID号）, imsi（设备IMSI号）, user（用户类型：1爸爸，2妈妈）, sys（血压高压）, dia（血压低压）, pul（心跳）, ano（心律不齐：0正常，1不正常）, time（服务器时间（格式：yyyyMMddHHmmss））, create_time（数据创建时间）
- `iot_receive_bp_maibobo_log` 智能血压计数据表。列：id（主键ID）, imei（设备IMEI号）, create_time（数据创建时间）, heart_rate（心率 (次/分钟)）, systolic_pressure（收缩压 (高压)）, diastolic_pressure（舒张压 (低压)）, is_message_type（消息类型）
- `iot_receive_crutch_log` 物联网智能拐杖实时数据。列：id（主键ID）, action（动作类型）, deviceid（设备ID）, type（报警类型：2=SOS报警，4=跌倒报警等）, longitude（经度）, latitude（纬度）, lbs_info（基站信息）, address（地址描述）, address_type（位置类型）, name（名字信息）, mt（坐标系）, battery（电量）, create_time（数据创建时间）
- `iot_receive_ctd_alarm_handle_log` 驰通达报警消息处理记录。列：id, uuid（数据uuid）, imei（设备的唯一标识）, device_type（设备类型）, alarm_reason（确认报警原因）, event_name（事件名称）, handler（处理人）, handle_time（处理时间）, alarm_time（报警时间）, remark（备注说明）, nonce（随机数）, signature（签名）, timestamp（时间戳）, mq_push_type（mq消息推送类型）
- `iot_receive_ctd_alarm_log` 驰通达报警记录。列：id（主键ID）, data_type（数据类型，如0表示心跳）, imei（设备IMEI）, imsi（设备imsi）, longitude（经度）, latitude（纬度）, iccid（设备ICCID）, sign_time（数据签名时间）, enabled_time（设备启用时间）, signature（签名值）, event_name（事件名称，如 心跳）, nonce（随机字符串）, site（安装点位）, device_type（设备类型，如 水浸）
- `iot_receive_gear_fit_log` 智能手环实时数据。列：id（自增主键ID）, message_type（消息类型 (DeviceMessageType类)）, device_id（设备ID）, time（时间）, icc_id（IM 卡 ICCID）, serial_number（流水号 yyyyMMddHHmmss + 4位循环数(0~9999)）, message_length（报文长度）, style_of_request（请求报文体）, message_identification（报文类型 (1:下发请求, 2:下发响应, 3:上报请求, 4:上报响应)）, protocol_version（平台协议版本号）, raw_message（原始消息内容）, current_battery（当前电量百分比）, current_steps（当前步数）, longitude（经度）
- `iot_receive_mattress_alarm_log` 物联网智能床垫报警记录。列：id（自增主键，便于管理）, sn（预警SN号（设备唯一标识））, warn_type（预警状态值1=心率过速，2=心率过缓，3=呼吸过速，4=呼吸过缓，5=离床预警，6=一键预警，7=离线预警）, warn_desc（备注信息）, warn_time（预警时间（设备端上报时间））, device_type（设备类型0=单人床数据，1=双人床左床，2=双人床右床）, uid（关联用户ID（新增业务字段））, create_time
- `iot_receive_mattress_log` 物联网设备指标数据表。列：id（自增主键，便于管理）, device_sn（设备序列号）, timestamp（时间戳（秒级））, heart_rate（心率）, breath_rate（呼吸率）, status（人体状态）, battery_level（电量百分比（0-100））, create_time（创建时间）
- `iot_receive_mattress_report_log` 物联网睡眠报告推送记录。列：id（主键）, sn（报告sn号）, total_duration（睡眠总时长，单位分钟）, inbed_duration（在床时长，单位分钟）, gobed_time（上床时间）, outbed_time（离床时间）, outbed_count_value（离床次数）, outbed_count_state（离床评分状态值）, outbed_count_desc（离床评分状态描述）, move_count_value（体动次数）, move_count_state（体动评分状态值）, move_count_desc（体动评分状态描述）, snoring_count_value（打鼾次数值）, snoring_count_state（打鼾评分状态值）
- `iot_receive_tigear_log` 钛极床垫接收记录。列：id（自增主键）, msg（消息体）, nonce（加密签名）, signature（签名随机串）, time（时间戳）, create_time（创建时间）, code（设备编号）
- `iot_receive_tuya_log` 物联网设备上报数据表。列：id（主键ID）, data_id（数据ID）, dev_id（设备ID）, product_id（产品ID）, properties（属性列表，存储为JSON格式）, report_type（上报类型）, create_time（创建时间）
- `monitor_room` 监测房间管理。列：id（主键）, create_by（创建人）, create_time（创建时间）, update_by（修改人）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, name（监测名称）, page_link（页面跳转链接）, title（标题）, arr_monitor_device_type（使用设备类型）, arr_user_id（绑定用户）
- `monitor_room_cohort` 监测房间-课程。列：id（主键）, create_by（创建人）, create_time（创建时间）, update_by（修改人）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, name（课程名称）, monitor_room_id（实训室id）, arr_user_id（绑定用户）
- `monitor_room_device` 监测房间设备管理。列：id（主键）, create_by（创建人）, create_time（创建时间）, update_by（修改人）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, monitor_room_id（监测房间ID）, device_id（设备ID）, remark（备注）, temp_json（模板参数）
- `monitor_room_use_log` 实训室使用记录。列：id（用户ID）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID 也就是所属机构）, monitor_id（实训室ID）, monitor_room_cohort_id（课程）, is_arr_monitor_device_type（使用设备类型）, use_id（使用者ID）, duration（时长（分钟））, remark（备注）, is_handle_status（处理状态）
- `org_room_type` 空间管理-房间类型。列：id（主键）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID 也就是所属机构）, code（自理编号）, name（自理名称）
- `org_space` 空间结构表（楼栋/楼层/房间/床位）。列：id（主键ID）, parent_id（父节点ID（为空表示顶级））, name（空间名称）, is_space_level（空间层级）, child_count（子级数量）, tenant_id（租户ID）, sort（排序字段，值越小越靠前）, remark（备注）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, room_type_id（房间类型ID）
- `person_crowd_type` 人员管理-自理分类。列：id（主键）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID 也就是所属机构）, code（自理编号）, name（自理名称）
- `person_emergency_contact` 人员信息-紧急联系人信息。列：id（主键;）, person_id（所属人员ID;）, alarmer_id（接警人ID）, concat_name_first（紧急联系人1）, concat_phone_first（紧急联系人电话）, concat_name_second（紧急联系人2）, concat_phone_second（紧急联系人2电话）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）
- `person_health_records` 人员信息-个人健康数据记录表。列：id（主键ID）, person_id（人员ID）, height（身高(cm)）, weight（体重(kg)）, heart_rate（心率 (次/分钟)）, blood_oxygen（血氧饱和度(%)）, temperature（温度 (摄氏度)）, blood_pressure（血压 (高压,低压)）, systolic_pressure（收缩压(mmHg)）, blood_glucose（血糖值 (mmol/L)）, diastolic_pressure（舒张压(mmHg)）, bone_mass（骨量）, bmi（BMI指数）, bmr（基础代谢率(卡路里/日)）
- `person_info` 人员基本信息。列：id（主键;）, name（姓名）, photo_file_id（头像照片ID）, is_gender（性别;0未知 1男 2女）, age（年龄，根据出生日期填充的）, phone（联系电话）, birthday（出生日期）, id_card_no（身份证号）, address（详细地址）, provinces_and_cities（省市区）, is_labor（劳动情况  1有劳动能力 2无劳动能力）, crowd_type_id（人群分类）, selfcare_conditions_id（自理情况）, live_conditions_id（居住情况）
- `person_life_conditions` 人员管理-生活情况。列：id（主键）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID 也就是所属机构）, code（自理编号）, name（自理名称）
- `person_lifestyle_habits` 人员信息-生活习惯。列：id（主键;）, person_id（所属人员ID;）, is_eat_habit（饮食习惯）, is_drink_habit（饮酒习惯）, is_smoke_habit（日常抽烟习惯）, is_arr_sport（体育锻炼习惯）, is_sleep_habit（日常睡眠情况）, is_yn_use_medicine（是否长期用药）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）
- `person_live_conditions` 人员管理-居住情况。列：id（主键）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID 也就是所属机构）, code（自理编号）, name（自理名称）
- `person_medical_history` 人员信息-遗传病史。列：id（主键;）, person_id（所属人员ID;）, genetic_disease_content（遗传病史）, past_medical_content（既往病史）, is_arr_allergy（药物过敏史）, is_arr_family（家族病史）, is_arr_chronic（慢性病）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）
- `person_physical` 人员信息-身体状况。列：id（主键;）, person_id（所属人员ID;）, is_health_con（健康状况评价）, is_selfcare_con（自理能力自评）, is_arr_physical_con（身体状况）, is_arr_psychology（心理特征）, is_arr_dementia（痴呆前兆）, is_arr_symptom（现存症状）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）
- `person_selfcare_conditions` 人员管理-自理情况。列：id（主键）, create_by（创建人ID）, create_time（创建时间）, update_by（修改人ID）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID 也就是所属机构）, code（自理编号）, name（自理名称）
- `remote_activity_foot_log` 实训室102-康乐活动策划-足底压力-检测记录。列：id（主键）, create_by（创建人）, create_time（创建时间）, update_by（修改人）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, check_type（体检类型）, left_arcIndex（左-足弓指）, left_footwidth（左-脚宽）, left_footheight（左-脚长）, right_arcIndex（右-足弓指）, right_footwidth（右-脚宽）, right_footheight（右-脚长）
- `remote_activity_foot_measure_log` 实训室102-康乐活动策划-足底压力-检测记录-区域信息。列：id（主键）, create_by（创建人）, create_time（创建时间）, update_by（修改人）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, foot_log_id（检测记录ID）, is_measure_type（区域类型）, average_pressure（压力平均值）, max_pressure（压力最大值）, total_pressure（压力总值）, total_area（总面积）, centroid（重心坐标）
- `remote_cockpit_project_log` 实训室203~206-智慧人力考评室（驾驶舱）-项目记录。列：id（主键）, create_by（创建人）, create_time（创建时间）, update_by（修改人）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, student_name（学生姓名）, student_sex（学生性别）, student_number（学号）, project_name（项目名称）, start_time（开始时间）, end_time（结束时间）, project_score（项目分数）
- `remote_health_bone_log` 实训室107-智慧健康管理-骨密度仪测量记录。列：id（主键）, create_by（创建人）, create_time（创建时间）, update_by（修改人）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, code（编号）, person_name（受检者姓名）, determination_time（测量时间）, t_value（T值）, z_value（Z值）, conclusion（结论）, doctor_name（医生）
- `remote_medicine_patient_log` 实训室108-中医科普馆-就诊记录。列：id（主键）, create_by（创建人）, create_time（创建时间）, update_by（修改人）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, bar_code（病例编号）, doctor_id（医生用户编号）, patient_name（姓名）, patient_sex（性别）, patient_age（年龄）, patient_birth（生日）, patient_phone（电话）
- `remote_nursing_chronic` 实训室120-老年护理-慢性病检测记录。列：id（主键）, create_by（创建人）, create_time（创建时间）, update_by（修改人）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, pdf_file_id（pdf文件id）, cus_id（客户id）, cus_name（客户姓名）, cus_birth（客户生日）, cus_sex（客户性别）, cus_mobile（客户手机号）, cus_card_no（客户身份证号）
- `remote_psychology_mood` 实训室104-智慧心理护理-情绪识别仪检测记录。列：id（主键）, create_by（创建人）, create_time（创建时间）, update_by（修改人）, update_time（修改时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, student_id（学生ID）, name（姓名）, sex（性别）, age（年龄）, height（身高）, weights（体重）, dev_id（设备号）
- `sys_department` 。列：DepartmentId, DepartmentName, DepartmentCode, ParentId, DepartmentType, IsEnable, Remark
- `sys_dept` 部门表。列：id（部门id）, parent_id（父部门id）, ancestors（祖级列表）, dept_name（部门名称）, order_num（显示顺序）, leader（负责人名称）, phone（联系电话）, email（邮箱）, create_by（创建者）, create_time（创建时间）, update_by（更新者）, update_time（更新时间）, deleted（逻辑删除标志）, tenant_id（租户ID 也就是所属机构）
- `sys_menu` 。列：Menu_Id, MenuName, Auth, Icon, Description, Enable, OrderNo, TableName, ParentId, Url
- `sys_role` 。列：Role_Id, DeleteBy, DeptName, Dept_Id, IsEnable, OrderNo, ParentId, RoleName
- `sys_user` 。列：User_Id, Address, AppType, AuditDate, AuditStatus, Auditor, Email, Enable, Gender, HeadImageUrl, IsRegregisterPhone
- `teaching_course_details` 教学-课程明细。列：id（id）, create_by（创建者）, create_time（创建时间）, update_by（更新者）, update_time（更新时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, course_id（所属课程ID）, is_course_details_type（课程类型）, is_experiment_type（仿真实验分类）, is_experiment_option_type（仿真实验项目）, course_title（课程标题）, chapter_name（章节名称）, sort（排序号）
- `teaching_course_info` 教学-课程。列：id（id）, create_by（创建者）, create_time（创建时间）, update_by（更新者）, update_time（更新时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, course_name（课程名称）, is_yn_open（是否开放）, lecturer_user_id（代理授课人）, begin_time（开始时间）, end_time（结束时间）, user_count（课程人数）, arr_problem_id（绑定题库id列表）
- `teaching_course_user` 教学-课程用户。列：id（id）, create_by（创建者）, create_time（创建时间）, update_by（更新者）, update_time（更新时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, course_id（课程id）, user_id（用户id）, study_count（学习次数）, study_begin_time（开始学习时间）, study_end_time（结束学习时间）, study_duration（学习时长（秒））, arr_studied_problem_title_id（已经学习的题库试题id列表）
- `teaching_course_user_thought` 教学-课程用户-观后感。列：id（id）, create_by（创建者）, create_time（创建时间）, update_by（更新者）, update_time（更新时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, course_details_id（课程明细id）, user_id（用户id）, thought_content（观后感）
- `teaching_exam_info` 教学-考试。列：id（id）, create_by（创建者）, create_time（创建时间）, update_by（更新者）, update_time（更新时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, exam_name（考试组卷名称）, exam_begin_time（开始时间）, exam_end_time（结束时间）, exam_duration（考试时长）, is_yn_open（是否开放）, is_yn_analysis（是否可以查看解析）, total_score（总分）
- `teaching_exam_title` 教学-考试-题目。列：id（id）, create_by（创建者）, create_time（创建时间）, update_by（更新者）, update_time（更新时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, exam_id（所属考试id）, is_problem_title_type（题目类型）, problem_title_name（题目名称）, sort（序号）, is_arr_problem_title_option_type（正确答案选项列表）, problem_title_analysis（题目解析）, exam_title_score（得分）
- `teaching_exam_title_option` 教学-考试-题目-选项。列：id（id）, create_by（创建者）, create_time（创建时间）, update_by（更新者）, update_time（更新时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, exam_title_id（所属题目ID）, is_problem_title_option_type（答案编号）, problem_title_option_content（答案内容）
- `teaching_exam_user` 教学-考试-用户。列：id（id）, create_by（创建者）, create_time（创建时间）, update_by（更新者）, update_time（更新时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, exam_id（考试id）, user_id（用户id）, score（得分）, is_exam_status（考试状态）
- `teaching_material_catalogue` 教学-资源目录。列：id（id）, create_by（创建者）, create_time（创建时间）, update_by（更新者）, update_time（更新时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, parent_id（父部门id）, ancestors（祖级列表）, catalogue_code（目录编码）, catalogue_name（目录名称）, order_num（显示顺序）
- `teaching_material_info` 教学-资源信息。列：id（id）, create_by（创建者）, create_time（创建时间）, update_by（更新者）, update_time（更新时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, is_material_file_type（资源文件类型）, is_material_classify（资源分类）, cover_file_id（封面图id）, arr_material_file_id（文件id）, title（标题）, remark（说明）, material_catalogue_id（资源目录id）
- `teaching_material_log` 教学-资源上传记录。列：id（id）, create_by（创建者）, create_time（创建时间）, update_by（更新者）, update_time（更新时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, file_id（文件id）, file_path（文件路径）
- `teaching_problem_info` 教学-题库。列：id（id）, create_by（创建者）, create_time（创建时间）, update_by（更新者）, update_time（更新时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, problem_name（题库名称）, is_yn_open（是否开放）
- `teaching_problem_title` 教学-题库-题目。列：id（id）, create_by（创建者）, create_time（创建时间）, update_by（更新者）, update_time（更新时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, problem_id（所属题库id）, is_problem_title_type（题目类型）, problem_title_name（题目名称）, sort（序号）, is_arr_problem_title_option_type（正确答案选项列表）, problem_title_analysis（题目解析）, option_json
- `teaching_problem_title_option` 教学-题库-题目-选项。列：id（id）, create_by（创建者）, create_time（创建时间）, update_by（更新者）, update_time（更新时间）, deleted（逻辑删除标志）, tenant_id（租户ID）, problem_title_id（所属题目ID）, is_problem_title_option_type（选项类型）, problem_title_option_content（选项内容）

## 常用 JOIN

- ：`bed_info`, `elderly_info`。`bed_info.elderly_id = elderly_info.id`
- ：`bed_info`, `floor_info`。`bed_info.floor_id = floor_info.id`

## 不在默认范围

`build_notifyallpeople`、`build_notifydept`、`build_notifypeople`、`build_staticpage`、`dify_knowledge_doc`、`dify_knowledge_doc_segment`、`dify_knowledge_type`、`formcollectionobject`、`formdesignoptions`、`gen_table`、`gen_table_column`、`iccm_card`、`iccm_card_phone`、`pers_evaluate_info`、`sys_authorizedmachines`、`sys_city`、`sys_codingrules`、`sys_dictionary`、`sys_dictionarylist`、`sys_file_management`、`sys_lbs_location`、`sys_log`、`sys_province`、`sys_quartzlog`、`sys_quartzoptions`、`sys_role_menu`、`sys_roleauth`、`sys_roleauthdata`、`sys_tablecolumn`、`sys_tableinfo`、`sys_tenant_info`、`sys_user_role`、`sys_userdepartment`、`sys_workflow`、`sys_workflowstep`、`sys_workflowtable`、`sys_workflowtableauditlog`、`sys_workflowtablestep`
